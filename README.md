### Git&Github Hands-On
안녕하세요, 김광회입니다.
지금까지 배운 내용을 바탕으로 해당 Repository를 `clone`하여, 개인의 `branch`를 만들고, master branch로 `merge` 한뒤 `push`하는 과제를 수행해봅니다.

---

> 주의! push하기 전에는 pull을 통해, Remote Repository로 부터 최신 정보를 업데이트해야합니다.

```bash
$ git status
$ git pull origin master
$ git push origin master
```
